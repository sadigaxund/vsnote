# dependencies
node_modules

# claude code local state
.claude/

# build output
dist
dist-ssr

# local env
.env
.env.local

# editor
.vscode/*
!.vscode/extensions.json
.idea

# logs
*.log
npm-debug.log*

# os
.DS_Store

# typescript build info
*.tsbuildinfo

# test artifacts
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# server/ (Python backend, Phase 9 — see server/README.md)
server/.venv/
*.db
__pycache__/
.pytest_cache/

# server/ (Phase 11 — bare git repos, VSNOTE_GIT_ROOT default)
server/git-repos/
# Phase 17 Milestone B — mirror-remote SSH keys/tokens, VSNOTE_SECRETS_PATH
# default. Never commit this — see app/secrets_store.py's module docstring.
server/secrets/
nice.png

# Vendored skill bodies (fetched by scripts/update-skill-references.mjs; pinned via skills/references.lock.json)
skills/references/
